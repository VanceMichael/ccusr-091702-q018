[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "student-custody"
version = "0.1.0"
description = "跨地研学监护协调站后端服务"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["app", "migrate"]