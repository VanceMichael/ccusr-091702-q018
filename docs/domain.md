# 领域约定

跨地研学监护协调站围绕学生分组、带队交接、活动同意和离线点名保存业务事实。主要对象为项目批次、学生、监护关系、分组行程、照护交接、活动同意、点名、紧急解封。

必须长期遵守以下约束：换组不能产生无人负责或双重负责；离线点名不能倒退人工确认；紧急资料限时解封；未点名按期限升级。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。